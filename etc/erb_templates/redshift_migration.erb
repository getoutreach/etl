require 'etl/migration/redshift'
require 'singleton'

module Migration
  class <%= @table %><%= @version %> < ETL::Migration::Redshift
    include Singleton

    def up
      <%= @up %>
    end

    def down
      <%= @down %>
    end
  end

  def self.execute
    <%= @table %><%= @version %>.instance.execute
  end
end
